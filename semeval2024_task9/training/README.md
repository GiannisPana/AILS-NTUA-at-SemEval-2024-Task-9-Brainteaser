# Training

The original notebooks remain the primary reproduction path. This package
contains extracted encoder scaffolding that can be imported on CPU. QLoRA
training is intentionally deferred until a GPU environment can verify it.
